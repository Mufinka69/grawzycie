#pragma once


#include <iostream>
#include <raylib.h>
#include <cstdlib>
#include <cstring>

#include "raygui.h"


#define WINDOW_WIDTH 800
#define WINDOW_HEIGHT 600

#define GAME_WIDTH 400
#define GAME_HEIGHT 300


// #define SCREEN_AT(i, j) (i)*WINDOW_HEIGHT+(j)

#define SPACE_AT(i, j) (i) * GAME_WIDTH + (j)

